{
  "_comment": "config.example.json for Engineering Digest for PMs",
  "workflow_id": "84-engineering-digest-for-pms",
  "workflow_name": "Engineering Digest for PMs",
  "collection": "09-engineering-ai-reliability",
  "status": "beta",
  "configuration": {
    "DIGEST_EMAIL_TO": { "default": "", "description": "PM recipients", "required": "Required" },
    "DIGEST_APPROVER_EMAIL": { "default": "", "description": "PM approver. Defaults to DIGEST_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@example.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM engineering-to-PM translation" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and digest distribution" }
  ],
  "schedule": "Every Friday at 09:00 researcher timezone"
}
