{
  "_comment": "config.example.json for Tech Debt Register",
  "workflow_id": "83-tech-debt-register",
  "workflow_name": "Tech Debt Register",
  "collection": "09-engineering-ai-reliability",
  "status": "beta",
  "configuration": {
    "TECH_DEBT_EMAIL_TO": { "default": "", "description": "Engineering and PM recipients", "required": "Required" },
    "TECH_DEBT_APPROVER_EMAIL": { "default": "", "description": "PM approver. Defaults to TECH_DEBT_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@acme-analytics.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM debt scoring and prioritisation" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and team distribution" }
  ]
}
