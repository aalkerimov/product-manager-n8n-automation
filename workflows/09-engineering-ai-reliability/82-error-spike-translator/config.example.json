{
  "_comment": "config.example.json for Error Spike Translator",
  "workflow_id": "82-error-spike-translator",
  "workflow_name": "Error Spike Translator",
  "collection": "09-engineering-ai-reliability",
  "status": "beta",
  "configuration": {
    "ERROR_SPIKE_EMAIL_TO": { "default": "", "description": "PM and stakeholder recipients", "required": "Required" },
    "ERROR_SPIKE_APPROVER_EMAIL": { "default": "", "description": "Approver. Defaults to ERROR_SPIKE_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@example.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM technical-to-plain-English translation" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and stakeholder notification" }
  ]
}
