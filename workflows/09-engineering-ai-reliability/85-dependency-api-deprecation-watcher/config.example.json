{
  "_comment": "config.example.json for Dependency & API Deprecation Watcher",
  "workflow_id": "85-dependency-api-deprecation-watcher",
  "workflow_name": "Dependency & API Deprecation Watcher",
  "collection": "09-engineering-ai-reliability",
  "status": "beta",
  "configuration": {
    "DEPRECATION_EMAIL_TO": { "default": "", "description": "Engineering team recipients", "required": "Required" },
    "DEPRECATION_APPROVER_EMAIL": { "default": "", "description": "PM approver. Defaults to DEPRECATION_EMAIL_TO.", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@acme-analytics.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and engineering alert" }
  ],
  "ai_required": false,
  "schedule": "Every Monday at 08:00",
  "notes": "No LLM required. Version comparison is deterministic. Replace hardcoded latestVersions map in 'Check Package Versions' with real npm registry HTTP Request calls for production use."
}
