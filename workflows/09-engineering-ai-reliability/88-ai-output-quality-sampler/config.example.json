{
  "_comment": "config.example.json for AI Output Quality Sampler",
  "workflow_id": "88-ai-output-quality-sampler",
  "workflow_name": "AI Output Quality Sampler",
  "collection": "09-engineering-ai-reliability",
  "status": "beta",
  "configuration": {
    "QUALITY_SAMPLE_EMAIL_TO": { "default": "", "description": "Engineering and PM recipients", "required": "Required" },
    "QUALITY_SAMPLE_APPROVER_EMAIL": { "default": "", "description": "Approver. Defaults to QUALITY_SAMPLE_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM evaluator model (temperature 0.1 for consistency)", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@example.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM output quality evaluation" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and quality report distribution" }
  ]
}
