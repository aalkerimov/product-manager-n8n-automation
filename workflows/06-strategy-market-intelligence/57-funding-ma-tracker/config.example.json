{
  "_comment": "config.example.json for Funding and M&A Tracker",
  "workflow_id": "57-funding-ma-tracker",
  "workflow_name": "Funding and M&A Tracker",
  "collection": "06-strategy-market-intelligence",
  "status": "beta",
  "configuration": {
    "FUNDING_EMAIL_TO": { "default": "", "description": "Alert recipient email", "required": "Required" },
    "FUNDING_COMPETITORS": { "default": "CompetitorX,CompetitorY", "description": "Comma-separated competitor names to track", "required": "Required" },
    "FUNDING_SCHEDULE": { "default": "0 9 * * 2", "description": "Weekly cron (default: Tuesday 09:00)", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@acme-analytics.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM implications analysis" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and report delivery" }
  ]
}
