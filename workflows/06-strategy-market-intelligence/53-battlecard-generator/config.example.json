{
  "_comment": "config.example.json for Battlecard Generator",
  "workflow_id": "53-battlecard-generator",
  "workflow_name": "Battlecard Generator",
  "collection": "06-strategy-market-intelligence",
  "status": "beta",
  "configuration": {
    "BATTLECARD_EMAIL_TO": { "default": "", "description": "Sales team email for approved battlecards", "required": "Required" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@example.com", "description": "Sender address", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model. Must support JSON mode.", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM API calls" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and delivery emails" }
  ]
}
