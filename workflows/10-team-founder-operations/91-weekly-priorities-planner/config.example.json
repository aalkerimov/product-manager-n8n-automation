{
  "_comment": "config.example.json for Weekly Priorities Planner",
  "workflow_id": "91-weekly-priorities-planner",
  "workflow_name": "Weekly Priorities Planner",
  "collection": "10-team-founder-operations",
  "status": "beta",
  "configuration": {
    "PRIORITIES_EMAIL_TO": { "default": "", "description": "Team recipients", "required": "Required" },
    "PRIORITIES_APPROVER_EMAIL": { "default": "", "description": "PM approver (usually yourself). Defaults to PRIORITIES_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@example.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM weekly priority planning" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and team distribution" }
  ],
  "schedule": "Every Monday at 07:00"
}
