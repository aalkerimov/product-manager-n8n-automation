{
  "_comment": "config.example.json for Knowledge Gap Finder",
  "workflow_id": "96-knowledge-gap-finder",
  "workflow_name": "Knowledge Gap Finder",
  "collection": "10-team-founder-operations",
  "status": "beta",
  "configuration": {
    "KNOWLEDGE_GAP_EMAIL_TO": { "default": "", "description": "Team recipients", "required": "Required" },
    "KNOWLEDGE_GAP_APPROVER_EMAIL": { "default": "", "description": "Manager approver. Defaults to KNOWLEDGE_GAP_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@acme-analytics.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "Gap identification and learning path generation" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval gate and team distribution" }
  ],
  "recommended_cadence": "Quarterly, before planning"
}
