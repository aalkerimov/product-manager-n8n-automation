{
  "_comment": "config.example.json for Sprint Retrospective Analyser",
  "workflow_id": "63-sprint-retrospective-analyser",
  "workflow_name": "Sprint Retrospective Analyser",
  "collection": "07-roadmap-planning",
  "status": "beta",
  "configuration": {
    "RETRO_EMAIL_TO": { "default": "", "description": "Team recipient for approved retro report", "required": "Required" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@acme-analytics.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM retro theme extraction" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and team delivery" }
  ],
  "webhook_path": "/webhook/retro-analyse",
  "webhook_method": "POST"
}
