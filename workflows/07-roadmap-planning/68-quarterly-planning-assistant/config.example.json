{
  "_comment": "config.example.json for Quarterly Planning Assistant",
  "workflow_id": "68-quarterly-planning-assistant",
  "workflow_name": "Quarterly Planning Assistant",
  "collection": "07-roadmap-planning",
  "status": "beta",
  "configuration": {
    "PLANNING_APPROVER_EMAIL": { "default": "", "description": "CPO / Head of Product approver", "required": "Required" },
    "PLANNING_EMAIL_TO": { "default": "", "description": "Leadership recipients for approved brief", "required": "Required" },
    "AI_MODEL": { "default": "gpt-4o", "description": "LLM model. Recommend gpt-4o for planning quality.", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@acme-analytics.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM planning brief generation" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and leadership delivery" }
  ],
  "notes": "Use gpt-4o for this workflow. gpt-4o-mini may produce lower-quality planning recommendations for complex strategic contexts."
}
