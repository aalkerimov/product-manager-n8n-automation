{
  "_comment": "config.example.json for Roadmap Communication Planner",
  "workflow_id": "70-roadmap-communication-planner",
  "workflow_name": "Roadmap Communication Planner",
  "collection": "07-roadmap-planning",
  "status": "beta",
  "configuration": {
    "COMMPLAN_APPROVER_EMAIL": { "default": "", "description": "CPO / PM approver", "required": "Required" },
    "COMMPLAN_EMAIL_TO": { "default": "", "description": "Recipient for approved plan (PM team)", "required": "Required" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@example.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM communication plan generation" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and delivery" }
  ]
}
