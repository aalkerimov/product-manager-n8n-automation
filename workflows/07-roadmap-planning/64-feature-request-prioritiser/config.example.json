{
  "_comment": "config.example.json for Feature Request Prioritiser",
  "workflow_id": "64-feature-request-prioritiser",
  "workflow_name": "Feature Request Prioritiser",
  "collection": "07-roadmap-planning",
  "status": "beta",
  "configuration": {
    "FEATURE_EMAIL_TO": { "default": "", "description": "Report recipient", "required": "Required" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@example.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM clustering and prioritisation" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and report delivery" }
  ]
}
