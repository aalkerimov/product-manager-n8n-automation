{
  "_comment": "config.example.json for Jobs-to-be-Done Extractor",
  "workflow_id": "75-jtbd-extractor",
  "workflow_name": "Jobs-to-be-Done Extractor",
  "collection": "08-user-research-discovery",
  "status": "beta",
  "configuration": {
    "JTBD_EMAIL_TO": { "default": "", "description": "Product team recipients", "required": "Required" },
    "JTBD_APPROVER_EMAIL": { "default": "", "description": "PM approver. Defaults to JTBD_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@example.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM JTBD extraction" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and team delivery" }
  ]
}
