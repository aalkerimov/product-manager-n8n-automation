{
  "_comment": "config.example.json for Synthesis Report Builder",
  "workflow_id": "78-synthesis-report-builder",
  "workflow_name": "Synthesis Report Builder",
  "collection": "08-user-research-discovery",
  "status": "beta",
  "configuration": {
    "SYNTHESIS_EMAIL_TO": { "default": "", "description": "Leadership/product team distribution list", "required": "Required" },
    "SYNTHESIS_APPROVER_EMAIL": { "default": "", "description": "PM approver. Defaults to SYNTHESIS_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@acme-analytics.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM cross-study synthesis" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and leadership distribution" }
  ]
}
