{
  "_comment": "config.example.json for Persona Generator",
  "workflow_id": "74-persona-generator",
  "workflow_name": "Persona Generator",
  "collection": "08-user-research-discovery",
  "status": "beta",
  "configuration": {
    "PERSONA_EMAIL_TO": { "default": "", "description": "Product and design team recipients", "required": "Required" },
    "PERSONA_APPROVER_EMAIL": { "default": "", "description": "PM approver. Defaults to PERSONA_EMAIL_TO.", "required": "Optional" },
    "AI_MODEL": { "default": "gpt-4o-mini", "description": "LLM model", "required": "Required" },
    "OPENAI_BASE_URL": { "default": "https://api.openai.com", "description": "LLM base URL", "required": "Optional" },
    "NOTIFICATION_EMAIL_FROM": { "default": "automation@acme-analytics.com", "description": "Sender address", "required": "Optional" }
  },
  "n8n_credentials_required": [
    { "name": "YOUR_OPENAI_CREDENTIAL", "type": "httpHeaderAuth", "required": true, "purpose": "LLM persona synthesis" },
    { "name": "YOUR_SMTP_CREDENTIAL", "type": "smtp", "required": true, "purpose": "Approval and team delivery" }
  ]
}
